# Shared Prompt Templates

These files are shared Agentic Mesh prompt text. Runtime code should assemble
and validate prompts, but standing wording should live here rather than inside
Python source files.

Role-specific wording belongs in `config/roles/*.yaml`. Project-specific
overrides belong in each project's `agentic-mesh/project.yaml`.
