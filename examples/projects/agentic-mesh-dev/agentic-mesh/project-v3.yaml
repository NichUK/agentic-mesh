project_id: agentic-mesh-dev
name: Agentic Mesh Development V3
goal:
  description: Prove the V3 agent-owned runtime with OneDrive-backed work-item dossiers, Teams stakeholder routing, configured deployment targets, and agent-owned lifecycle progression.
  success_measures:
  - One dogfood slice reaches release and closure through V3 tools and evidence.
  - Work-item dossiers are written under the project document library `/documents/work-items`.
  - Sponsor approval and closure notifications use configured stakeholder contacts.
  - Release Manager deployment uses a configured deployment target.
  constraints:
  - Runtime state, project configuration, source repository, and document library remain separate.
  - Secrets and access tokens are supplied by runtime environment, not this file.
flow:
  template: sdlc
broker:
  adapter: nats-jetstream
  stream: agent-inbox
  servers: nats://v3-nats:4222
document_library:
  adapter: onedrive
  drive_id: ${AGENTIC_MESH_ONEDRIVE_DRIVE_ID}
  root_path: /documents
  structure_policy: togaf-sdlc-v1
target_repositories:
  agentic-mesh:
    type: git
    path: ../../..
    default_branch: develop
release_deployment_targets:
  dogfood-compose:
    type: command
    description: Build and activate the linuxch dogfood Docker Compose runtime from the checked-out system repository.
    command:
    - sh
    - scripts/release-linuxch-compose.sh
    working_directory: /mesh/system
    timeout_seconds: 900
    rollback_summary: Rebuild and activate the previous reviewed image or revert the scoped commit and rerun the dogfood Compose activation target.
stakeholder_contacts:
  sponsor:
    display_name: Nicholas Overend
    connector: teams
    target_ref: user:${AGENTIC_MESH_SPONSOR_TEAMS_USER_ID}
    importance: high
connectors:
  teams:
    adapter: teams-bot-connector
    identity_model: role_bots
    tenant_id: ${AGENTIC_MESH_TENANT_ID}
    graph_base_url: https://graph.microsoft.com/v1.0
    ingress:
      public_endpoint: ${AGENTIC_MESH_TEAMS_PUBLIC_ENDPOINT}
      listen_host: 0.0.0.0
      listen_port: 3978
      path: /teams/activity
    bot_service:
      resource_group: agentic-mesh-dev
      location: global
      sku: F0
      name_template: am-{role_id_safe}
      endpoint: ${AGENTIC_MESH_TEAMS_PUBLIC_ENDPOINT}
    role_bots:
      business-analyst:
        display_name: AM-Business Analyst
        bot_id_ref: teams-bot-business-analyst-app-id
        secret_ref: teams-bot-business-analyst-secret
      delivery-manager:
        display_name: AM-Delivery Manager
        bot_id_ref: teams-bot-delivery-manager-app-id
        secret_ref: teams-bot-delivery-manager-secret
      enterprise-architect:
        display_name: AM-Enterprise Architect
        bot_id_ref: teams-bot-enterprise-architect-app-id
        secret_ref: teams-bot-enterprise-architect-secret
      platform-engineer:
        display_name: AM-Platform Engineer
        bot_id_ref: teams-bot-platform-engineer-app-id
        secret_ref: teams-bot-platform-engineer-secret
      product-manager:
        display_name: AM-Product Manager
        bot_id_ref: teams-bot-product-manager-app-id
        secret_ref: teams-bot-product-manager-secret
      prompt-engineer:
        display_name: AM-Prompt Engineer
        bot_id_ref: teams-bot-prompt-engineer-app-id
        secret_ref: teams-bot-prompt-engineer-secret
      project-manager:
        display_name: AM-Project Manager
        bot_id_ref: teams-bot-project-manager-app-id
        secret_ref: teams-bot-project-manager-secret
      research-analyst:
        display_name: AM-Research Analyst
        bot_id_ref: teams-bot-research-analyst-app-id
        secret_ref: teams-bot-research-analyst-secret
      security-architect:
        display_name: AM-Security Architect
        bot_id_ref: teams-bot-security-architect-app-id
        secret_ref: teams-bot-security-architect-secret
      solution-architect:
        display_name: AM-Solution Architect
        bot_id_ref: teams-bot-solution-architect-app-id
        secret_ref: teams-bot-solution-architect-secret
      engineering:
        display_name: AM-Engineering
        bot_id_ref: teams-bot-engineering-app-id
        secret_ref: teams-bot-engineering-secret
      qa-engineer:
        display_name: AM-QA Engineer
        bot_id_ref: teams-bot-qa-engineer-app-id
        secret_ref: teams-bot-qa-engineer-secret
      release-manager:
        display_name: AM-Release Manager
        bot_id_ref: teams-bot-release-manager-app-id
        secret_ref: teams-bot-release-manager-secret
      technical-writer:
        display_name: AM-Technical Writer
        bot_id_ref: teams-bot-technical-writer-app-id
        secret_ref: teams-bot-technical-writer-secret
      ux-designer:
        display_name: AM-UX Designer
        bot_id_ref: teams-bot-ux-designer-app-id
        secret_ref: teams-bot-ux-designer-secret
    team:
      id: ${AGENTIC_MESH_PROJECT_TEAM_ID}
      name: dev-agentic-mesh
    channels:
      project:
        id: ${AGENTIC_MESH_PROJECT_CHANNEL_ID}
        name: project
    authority_groups:
      sponsors:
      - sponsor
      release-board:
      - release_approver
      operators:
      - operator
    people:
    - person_id: nicholas-overend
      display_name: Nicholas Overend
      external_refs:
      - ${AGENTIC_MESH_SPONSOR_AAD_OBJECT_ID}
      groups:
      - sponsors
      - release-board
      - operators
roles:
  business-analyst:
    template: business-analyst
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Own requirements analysis, stakeholder need capture, and requirements evidence before product or architecture commitments move forward.
    write_paths:
    - documents/work-items/**
  delivery-manager:
    template: delivery-manager
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Coordinate delivery flow, chase impediments, and ensure handoffs include required evidence and next actions.
    write_paths:
    - documents/work-items/**
  enterprise-architect:
    template: enterprise-architect
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Govern enterprise alignment, durable architecture direction, capability fit, and TOGAF-style traceability.
    write_paths:
    - documents/work-items/**
  platform-engineer:
    template: platform-engineer
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Own platform, runtime, deployment substrate, operational tooling, and infrastructure evidence.
    write_paths:
    - deploy/**
    - config/**
    - documents/work-items/**
  product-manager:
    template: product-manager
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Shape product scope, acceptance criteria, and sponsor sign-off before downstream work.
    write_paths:
    - documents/work-items/**
  prompt-engineer:
    template: prompt-engineer
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Review and improve prompt contracts, role instructions, tool-use behaviour, and agent interaction quality.
    write_paths:
    - config/prompts/**
    - config/roles/**
    - documents/work-items/**
  project-manager:
    template: project-manager
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Keep work progressing and close only after required evidence is recorded.
    write_paths:
    - documents/work-items/**
  research-analyst:
    template: research-analyst
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Provide source-backed research, alternatives analysis, and uncertainty notes when evidence is required.
    write_paths:
    - documents/work-items/**
  security-architect:
    template: security-architect
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Govern secure design, NIST SSDF/OWASP-aligned controls, threat/risk evidence, and security release considerations.
    write_paths:
    - documents/work-items/**
  solution-architect:
    template: solution-architect
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Own solution design, integration shape, architectural trade-offs, and downstream implementation constraints.
    write_paths:
    - documents/work-items/**
  engineering:
    template: engineering
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Implement signed-off development slices and record source-linked evidence.
    write_paths:
    - src/**
    - tests/**
    - documents/work-items/**
  qa-engineer:
    template: qa-engineer
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Verify acceptance criteria and deployment evidence before release.
    write_paths:
    - tests/**
    - documents/work-items/**
  release-manager:
    template: release-manager
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Execute configured deployment targets and record rollback, smoke evidence, residual risk, and closure.
    write_paths:
    - documents/work-items/**
  technical-writer:
    template: technical-writer
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Produce clear durable documentation, release notes, operating notes, and document-library hygiene.
    write_paths:
    - documents/**
  ux-designer:
    template: ux-designer
    instances: 1
    worker:
      adapter: persistent-session
      model: gpt-5.5
      timeout_seconds: 300
      reasoning_effort: high
      sandbox_mode: danger-full-access
      auth:
        credential: codex-agentic-mesh-dev-team-q
    instructions:
    - Own user experience, interaction design, information hierarchy, and UX acceptance evidence for user-facing work.
    write_paths:
    - documents/work-items/**
