services:
  base-agent-image:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    build:
      context: ${AGENTIC_MESH_RUNTIME_BUILD_CONTEXT:-../../../../..}
      target: base-agent
    profiles:
      - build-image

  ops-agent-image:
    image: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
    build:
      context: ${AGENTIC_MESH_RUNTIME_BUILD_CONTEXT:-../../../../..}
      target: ops-agent
    profiles:
      - build-image

  dev-agent-image:
    image: ${AGENTIC_MESH_DEV_IMAGE_TAG:-agentic-mesh:dev-agent}
    build:
      context: ${AGENTIC_MESH_RUNTIME_BUILD_CONTEXT:-../../../../..}
      target: dev-agent
    profiles:
      - build-image

  qa-agent-image:
    image: ${AGENTIC_MESH_QA_IMAGE_TAG:-agentic-mesh:qa-agent}
    build:
      context: ${AGENTIC_MESH_RUNTIME_BUILD_CONTEXT:-../../../../..}
      target: qa-agent
    profiles:
      - build-image

  runtime:
    image: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
    env_file:
      - path: .env
        required: false
    command: python -m agentic_mesh_v4.cli --db /mesh/project/state/v4/agentic-mesh-v4.sqlite3 --project-config /mesh/project/agentic-mesh/project-v4.yaml serve --host 0.0.0.0 --port 8100 --document-root /documents
    ports:
      - "${AGENTIC_MESH_V4_STATUS_PORT:-8100}:8100"
    environment:
      AGENTIC_MESH_RUNTIME_VERSION: v4
      AGENTIC_MESH_URL_ROOT: ${AGENTIC_MESH_URL_ROOT:-http://linuxch:8100}
    volumes:
      - ${AGENTIC_MESH_SYSTEM_HOST_PATH:-../../../../..}:/mesh/system:ro
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - /var/run/docker.sock:/var/run/docker.sock

  dispatcher:
    image: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
    env_file:
      - path: .env
        required: false
    command: python -m agentic_mesh_v4.cli --db /mesh/project/state/v4/agentic-mesh-v4.sqlite3 --project-config /mesh/project/agentic-mesh/project-v4.yaml dispatch-loop --agent-config-root /mesh/project/state/v4/agent-configs --compose-file /mesh/project/deploy/compose/docker-compose.v4.yml --compose-file /mesh/project/deploy/compose/docker-compose.linuxch.yml --compose-project-name ${COMPOSE_PROJECT_NAME:-agentic-mesh} --compose-env-file /mesh/project/deploy/compose/.env --compose-working-directory /mesh/project/deploy/compose --active-turn-stale-seconds ${AGENTIC_MESH_ACTIVE_TURN_STALE_SECONDS:-900} --wake
    environment:
      AGENTIC_MESH_RUNTIME_VERSION: v4
      AGENTIC_MESH_URL_ROOT: ${AGENTIC_MESH_URL_ROOT:-http://linuxch:8100}
      AGENTIC_MESH_SYSTEM_HOST_PATH: ${AGENTIC_MESH_SYSTEM_HOST_PATH:-../../../../..}
      AGENTIC_MESH_PROJECT_HOST_PATH: ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}
      AGENTIC_MESH_WORKSPACE_HOST_PATH: ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}
      AGENTIC_MESH_CODEX_HOME_HOST_PATH: ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}
      AGENTIC_MESH_OTEL_COLLECTOR_CONFIG_HOST_PATH: ${AGENTIC_MESH_OTEL_COLLECTOR_CONFIG_HOST_PATH:-../../../../../config/otel/collector.yaml}
      AGENTIC_MESH_BASE_IMAGE_TAG: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
      AGENTIC_MESH_OPS_IMAGE_TAG: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
      AGENTIC_MESH_DEV_IMAGE_TAG: ${AGENTIC_MESH_DEV_IMAGE_TAG:-agentic-mesh:dev-agent}
      AGENTIC_MESH_QA_IMAGE_TAG: ${AGENTIC_MESH_QA_IMAGE_TAG:-agentic-mesh:qa-agent}
      COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME:-agentic-mesh}
    volumes:
      - ${AGENTIC_MESH_SYSTEM_HOST_PATH:-../../../../..}:/mesh/system:ro
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - /var/run/docker.sock:/var/run/docker.sock

  watchdog:
    image: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
    env_file:
      - path: .env
        required: false
    command: python -m agentic_mesh_v4.cli --db /mesh/project/state/v4/agentic-mesh-v4.sqlite3 --project-config /mesh/project/agentic-mesh/project-v4.yaml watchdog-loop --compose-file /mesh/project/deploy/compose/docker-compose.v4.yml --compose-file /mesh/project/deploy/compose/docker-compose.linuxch.yml --compose-project-name ${COMPOSE_PROJECT_NAME:-agentic-mesh} --compose-env-file /mesh/project/deploy/compose/.env --compose-working-directory /mesh/project/deploy/compose --service runtime --service dispatcher --poll-interval-seconds ${AGENTIC_MESH_WATCHDOG_INTERVAL_SECONDS:-30}
    environment:
      AGENTIC_MESH_RUNTIME_VERSION: v4
      AGENTIC_MESH_URL_ROOT: ${AGENTIC_MESH_URL_ROOT:-http://linuxch:8100}
      AGENTIC_MESH_SYSTEM_HOST_PATH: ${AGENTIC_MESH_SYSTEM_HOST_PATH:-../../../../..}
      AGENTIC_MESH_PROJECT_HOST_PATH: ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}
      AGENTIC_MESH_WORKSPACE_HOST_PATH: ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}
      AGENTIC_MESH_CODEX_HOME_HOST_PATH: ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}
      AGENTIC_MESH_OTEL_COLLECTOR_CONFIG_HOST_PATH: ${AGENTIC_MESH_OTEL_COLLECTOR_CONFIG_HOST_PATH:-../../../../../config/otel/collector.yaml}
      AGENTIC_MESH_BASE_IMAGE_TAG: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
      AGENTIC_MESH_OPS_IMAGE_TAG: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
      AGENTIC_MESH_DEV_IMAGE_TAG: ${AGENTIC_MESH_DEV_IMAGE_TAG:-agentic-mesh:dev-agent}
      AGENTIC_MESH_QA_IMAGE_TAG: ${AGENTIC_MESH_QA_IMAGE_TAG:-agentic-mesh:qa-agent}
      COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME:-agentic-mesh}
    volumes:
      - ${AGENTIC_MESH_SYSTEM_HOST_PATH:-../../../../..}:/mesh/system:ro
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - /var/run/docker.sock:/var/run/docker.sock

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.101.0
    command: ["--config=/etc/otelcol/config.yaml"]
    volumes:
      - ${AGENTIC_MESH_OTEL_COLLECTOR_CONFIG_HOST_PATH:-../../../../../config/otel/collector.yaml}:/etc/otelcol/config.yaml:ro
    ports:
      - "4317:4317"
      - "4318:4318"

  agentic-mesh-dev-project-manager-1:
    image: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: sh -lc 'mkdir -p /root/.ssh; if [ -d /mesh/home/.ssh ]; then cp -r /mesh/home/.ssh/. /root/.ssh/; fi; if [ -f /root/.ssh/config ]; then sed -i "s#/mesh/home/.ssh#/root/.ssh#g" /root/.ssh/config; fi; chmod 700 /root/.ssh; find /root/.ssh -type f -exec chmod 600 {} \; 2>/dev/null || true; exec codex app-server --listen ws://0.0.0.0:4700 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token'
    environment:
      AGENTIC_MESH_ROLE_ID: project-manager
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.project-manager.1
      AGENTIC_MESH_TOOL_PROFILE: ops-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/project-manager/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex
      - /var/run/docker.sock:/var/run/docker.sock
      - ${AGENTIC_MESH_PROJECT_ENV_FILE_HOST_PATH:-.env}:/mesh/home/.env:ro
      - ${AGENTIC_MESH_PROJECT_MANAGER_SSH_HOST_PATH:-../../state/worker_mounts/project-manager/.ssh}:/mesh/home/.ssh:ro

  agentic-mesh-dev-delivery-manager-1:
    image: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: sh -lc 'mkdir -p /root/.ssh; if [ -d /mesh/home/.ssh ]; then cp -r /mesh/home/.ssh/. /root/.ssh/; fi; if [ -f /root/.ssh/config ]; then sed -i "s#/mesh/home/.ssh#/root/.ssh#g" /root/.ssh/config; fi; chmod 700 /root/.ssh; find /root/.ssh -type f -exec chmod 600 {} \; 2>/dev/null || true; exec codex app-server --listen ws://0.0.0.0:4701 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token'
    environment:
      AGENTIC_MESH_ROLE_ID: delivery-manager
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.delivery-manager.1
      AGENTIC_MESH_TOOL_PROFILE: ops-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/delivery-manager/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex
      - /var/run/docker.sock:/var/run/docker.sock
      - ${AGENTIC_MESH_PROJECT_ENV_FILE_HOST_PATH:-.env}:/mesh/home/.env:ro
      - ${AGENTIC_MESH_PROJECT_MANAGER_SSH_HOST_PATH:-../../state/worker_mounts/project-manager/.ssh}:/mesh/home/.ssh:ro

  agentic-mesh-dev-product-manager-1:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4702 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: product-manager
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.product-manager.1
      AGENTIC_MESH_TOOL_PROFILE: base-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/product-manager/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex

  agentic-mesh-dev-business-analyst-1:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4703 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: business-analyst
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.business-analyst.1
      AGENTIC_MESH_TOOL_PROFILE: base-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/business-analyst/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex

  agentic-mesh-dev-research-analyst-1:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4704 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: research-analyst
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.research-analyst.1
      AGENTIC_MESH_TOOL_PROFILE: base-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/research-analyst/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex

  agentic-mesh-dev-enterprise-architect-1:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4705 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: enterprise-architect
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.enterprise-architect.1
      AGENTIC_MESH_TOOL_PROFILE: base-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/enterprise-architect/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex

  agentic-mesh-dev-solution-architect-1:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4706 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: solution-architect
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.solution-architect.1
      AGENTIC_MESH_TOOL_PROFILE: base-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/solution-architect/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex

  agentic-mesh-dev-security-architect-1:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4707 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: security-architect
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.security-architect.1
      AGENTIC_MESH_TOOL_PROFILE: base-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/security-architect/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex

  agentic-mesh-dev-ux-designer-1:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4708 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: ux-designer
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.ux-designer.1
      AGENTIC_MESH_TOOL_PROFILE: base-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/ux-designer/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex

  agentic-mesh-dev-engineering-1:
    image: ${AGENTIC_MESH_DEV_IMAGE_TAG:-agentic-mesh:dev-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4709 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: engineering
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.engineering.1
      AGENTIC_MESH_TOOL_PROFILE: dev-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/engineering/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex
      - /var/run/docker.sock:/var/run/docker.sock

  agentic-mesh-dev-qa-engineer-1:
    image: ${AGENTIC_MESH_QA_IMAGE_TAG:-agentic-mesh:qa-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4710 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: qa-engineer
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.qa-engineer.1
      AGENTIC_MESH_TOOL_PROFILE: qa-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/qa-engineer/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex

  agentic-mesh-dev-platform-engineer-1:
    image: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: sh -lc 'mkdir -p /root/.ssh; if [ -d /mesh/home/.ssh ]; then cp -r /mesh/home/.ssh/. /root/.ssh/; fi; if [ -f /root/.ssh/config ]; then sed -i "s#/mesh/home/.ssh#/root/.ssh#g" /root/.ssh/config; fi; chmod 700 /root/.ssh; find /root/.ssh -type f -exec chmod 600 {} \; 2>/dev/null || true; exec codex app-server --listen ws://0.0.0.0:4711 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token'
    environment:
      AGENTIC_MESH_ROLE_ID: platform-engineer
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.platform-engineer.1
      AGENTIC_MESH_TOOL_PROFILE: ops-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/platform-engineer/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex
      - /var/run/docker.sock:/var/run/docker.sock
      - ${AGENTIC_MESH_PROJECT_ENV_FILE_HOST_PATH:-.env}:/mesh/home/.env:ro
      - ${AGENTIC_MESH_PROJECT_MANAGER_SSH_HOST_PATH:-../../state/worker_mounts/project-manager/.ssh}:/mesh/home/.ssh:ro

  agentic-mesh-dev-release-manager-1:
    image: ${AGENTIC_MESH_OPS_IMAGE_TAG:-agentic-mesh:ops-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: sh -lc 'mkdir -p /root/.ssh; if [ -d /mesh/home/.ssh ]; then cp -r /mesh/home/.ssh/. /root/.ssh/; fi; if [ -f /root/.ssh/config ]; then sed -i "s#/mesh/home/.ssh#/root/.ssh#g" /root/.ssh/config; fi; chmod 700 /root/.ssh; find /root/.ssh -type f -exec chmod 600 {} \; 2>/dev/null || true; exec codex app-server --listen ws://0.0.0.0:4712 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token'
    environment:
      AGENTIC_MESH_ROLE_ID: release-manager
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.release-manager.1
      AGENTIC_MESH_TOOL_PROFILE: ops-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/release-manager/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex
      - /var/run/docker.sock:/var/run/docker.sock
      - ${AGENTIC_MESH_PROJECT_ENV_FILE_HOST_PATH:-.env}:/mesh/home/.env:ro
      - ${AGENTIC_MESH_PROJECT_MANAGER_SSH_HOST_PATH:-../../state/worker_mounts/project-manager/.ssh}:/mesh/home/.ssh:ro

  agentic-mesh-dev-technical-writer-1:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4713 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: technical-writer
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.technical-writer.1
      AGENTIC_MESH_TOOL_PROFILE: base-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/technical-writer/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex

  agentic-mesh-dev-prompt-engineer-1:
    image: ${AGENTIC_MESH_BASE_IMAGE_TAG:-agentic-mesh:base-agent}
    env_file:
      - path: .env
        required: false
    profiles:
      - roles
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    working_dir: /mesh/agent
    command: codex app-server --listen ws://0.0.0.0:4714 --ws-auth capability-token --ws-token-file /mesh/agent/ws-token
    environment:
      AGENTIC_MESH_ROLE_ID: prompt-engineer
      AGENTIC_MESH_ROLE_INSTANCE_ID: agentic-mesh-dev.prompt-engineer.1
      AGENTIC_MESH_TOOL_PROFILE: base-agent
      CODEX_HOME: /mesh/worker-auth/codex
      HOME: /mesh/home
    volumes:
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}/state/v4/agent-configs/prompt-engineer/1:/mesh/agent
      - ${AGENTIC_MESH_DOCUMENTS_HOST_PATH:-../documents}:/documents
      - ${AGENTIC_MESH_PROJECT_HOST_PATH:-../..}:/mesh/project
      - ${AGENTIC_MESH_WORKSPACE_HOST_PATH:-../../../../..}:/mesh/workspaces/agentic-mesh
      - ${AGENTIC_MESH_CODEX_HOME_HOST_PATH:-../../state/worker_mounts/codex-agentic-mesh-dev-team-home-q}:/mesh/worker-auth/codex
